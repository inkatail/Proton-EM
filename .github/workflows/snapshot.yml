name: Snapshot

on:
  workflow_dispatch:
  push:
    branches:
      - 'em-*'

concurrency:
  group: ${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  version:
    name: Version
    runs-on: ubuntu-slim
    outputs:
      tag_abbrev: ${{ steps.version.outputs.tag_abbrev }}
      tag_offset: ${{ steps.version.outputs.tag_offset }}
      sha_short: ${{ steps.version.outputs.sha_short }}
      full_desc: ${{ steps.version.outputs.full_desc }}
      branch: ${{ steps.version.outputs.branch }}
    steps:
    - name: Checkout
      uses: actions/checkout@v6
      with:
        fetch-depth: 0
        fetch-tags: true

    - name: Version
      id: version
      shell: bash
      run: |
        if tag_abbrev=$(git describe --tags --abbrev=0 2>/dev/null); then
          tag_offset=$(git rev-list "$tag_abbrev"..HEAD --count)
          full_desc=$(git describe --long --tags)
        else
          tag_abbrev="snapshot"
          tag_offset="0"
          full_desc="snapshot-$(git rev-parse --short HEAD)"
        fi

        echo "tag_abbrev=$tag_abbrev" >> "$GITHUB_OUTPUT"
        echo "tag_offset=$tag_offset" >> "$GITHUB_OUTPUT"
        echo "sha_short=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"
        echo "full_desc=$full_desc" >> "$GITHUB_OUTPUT"
        echo "branch=${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}" >> "$GITHUB_OUTPUT"

  build:
    name: Build
    needs: version
    strategy:
      matrix:
        include:
          - name: proton-${{ needs.version.outputs.full_desc }}-x86_64
            cflags: -O2 -march=nocona -mtune=core-avx2
            rustflags: -Copt-level=3 -Ctarget-cpu=nocona

          - name: proton-${{ needs.version.outputs.full_desc }}-arm64
            configure: >-
              --target-arch=arm64
            runner: 'ubuntu-24.04-arm'
    uses: ./.github/workflows/_job_build.yml
    with:
      name: ${{ matrix.name }}
      cflags: ${{ matrix.cflags }}
      rustflags: ${{ matrix.rustflags }}
      configure: --enable-ccache ${{ matrix.configure }}
      runner: ${{ matrix.runner != '' && matrix.runner || 'ubuntu-24.04' }}
      continue: true

  test:
    name: Test
    needs: [version, build]
    strategy:
      matrix:
        include:
          - name: proton-${{ needs.version.outputs.full_desc }}-x86_64
          - name: proton-${{ needs.version.outputs.full_desc }}-arm64
            runner: 'ubuntu-24.04-arm'
    uses: ./.github/workflows/_job_test.yml
    with:
      name: ${{ matrix.name }}
      runner: ${{ matrix.runner != '' && matrix.runner || 'ubuntu-24.04' }}
      continue: true
